const superHeros: number[] = [];
const heroName: Array<string> = [];

superHeros.push(1);
heroName.push("SpiderMan");

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

const MlModels: number[][] = [[255, 255, 255], []];

const oneUsers: User[] = [];

allUsers.push({ name: "Kopa", isActive: false });
